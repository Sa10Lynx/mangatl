node_modules/

# Build output (generated via `npm run build`, see README.md). Not committed -- regenerate locally.
dist/

# Designated local-only path for logging.ts's opt-in raw-byte dump (CLAUDE.md invariant 3).
# Anything written here is for manual review only and must never be committed.
.local-only-dumps/
